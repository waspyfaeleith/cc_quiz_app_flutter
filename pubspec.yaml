name: cc_quiz_app
description: CodeClan Revision Quiz App

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  quiz_view: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  test: ^1.16.0

flutter:
  uses-material-design: true